const { override, /* adicione aqui os plugins necessários */ } = require('customize-cra');

module.exports = override(
  // Copie as personalizações do seu craco.config.js para aqui
  // Por exemplo:
  // addLessLoader({
  //   lessOptions: {
  //     javascriptEnabled: true,
  //   },
  // }),
);
